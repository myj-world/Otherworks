<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IGCSEAccountingPlatform" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="21">@color/purple_700</item>
    </style>

    <style name="Theme.IGCSEAccountingPlatform" parent="Base.Theme.IGCSEAccountingPlatform" />
</resources>